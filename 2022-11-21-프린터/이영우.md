아직 의사 코드 작성하면서 해결하지 못했음.

```javascript
function solution(priorities, location) {
    var answer = 0;
    
    // 출력하는 문서 수
    let printCnt = 0;
    // 우선순위가 높은 문서가 대기 목록에 있을 경우 === 1 !== 0
    let printWait = 0;
    
    // 반복문
    // 문서 수가 0 보다 크다면 계속 반복
    // 만약에 priorities[0] 이 priorities[i] 보다 작다면 printWait 을 1로 지정하고 break;
    // 만약에 printWait === 1 이라면, 
    return answer;
}
```
